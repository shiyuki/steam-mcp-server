---
status: diagnosed
phase: v1.1-milestone
source: ROADMAP.md success criteria (Phases 1-4)
started: 2026-02-12T23:22:00Z
updated: 2026-02-13T01:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Search games by genre tag
expected: search_genre with "Action" and limit 5 returns 5 AppIDs, total_found in thousands, includes fetched_at
result: pass

### 2. Tag normalization (unhyphenated input)
expected: search_genre with "Roguelike" normalizes to "Rogue-like" and returns thousands of games (not 70 bowling games)
result: pass

### 3. Unrecognized tag returns clear error
expected: search_genre with a completely made-up tag like "xyznonexistent123" returns an error with error_type "tag_not_found", not silent wrong data
result: issue
reported: "doesn't work, returns bowling games instead of an error"
severity: major

### 4. Fetch game metadata
expected: fetch_metadata for AppID 646570 (Slay the Spire) returns name, tags, developer, and description as structured data
result: pass

### 5. Invalid AppID returns error
expected: fetch_metadata with AppID 0 or -1 returns a clear error message, not a crash
result: pass

### 6. Fetch commercial data
expected: fetch_commercial for AppID 646570 returns price, revenue_min, revenue_max, confidence level, and data source
result: pass

### 7. Revenue shown as range with confidence
expected: fetch_commercial revenue is a min-max range (not a single number), with confidence field indicating data reliability
result: pass

### 8. Fetch engagement data
expected: fetch_engagement for AppID 646570 returns CCU, owner estimate, playtime (avg/median), review score, and derived health metrics (ccu_ratio, playtime_engagement, etc.)
result: pass

### 9. Aggregate engagement across genre
expected: aggregate_engagement with tag "Indie" returns statistical distribution (mean, median, p25, p75) for CCU, owners, playtime, review scores across the genre, plus per-game breakdown
result: issue
reported: "PUBG appears under Indie tag but Indie is not in PUBG's top 20 tags. Should only qualify games as befitting the searched tag if it's in the game's top 20 tag list"
severity: major

### 10. Data freshness tracking
expected: All tool responses include fetched_at (ISO timestamp) and cache_age_seconds (0 for fresh, >0 for cached) fields
result: pass

## Summary

total: 10
passed: 8
issues: 2
pending: 0
skipped: 0

## Gaps

- truth: "Unrecognized tag returns error with error_type tag_not_found, not silent wrong data"
  status: failed
  reason: "User reported: doesn't work, returns bowling games instead of an error"
  severity: major
  test: 3
  root_cause: "BOWLING_FALLBACK_APPIDS constant at steam_api.py:64 contains wrong AppIDs {436590, 212370, 340170} that don't appear in the actual SteamSpy bowling fallback dataset. Real fallback AppIDs are {901583, 12210, 2990, 891040, 22230}. Detection logic is correct but fingerprint data is wrong. Tests are circular — mock data uses the same wrong AppIDs."
  artifacts:
    - path: "src/steam_api.py"
      issue: "BOWLING_FALLBACK_APPIDS at line 64 contains wrong fingerprint AppIDs"
    - path: "tests/test_engagement.py"
      issue: "_create_bowling_fallback_data() at lines 1170-1202 uses same wrong AppIDs, circular validation"
  missing:
    - "Replace BOWLING_FALLBACK_APPIDS with real AppIDs from SteamSpy bowling fallback (e.g., 901583, 12210, 2990)"
    - "Update test helper to use real bowling fallback AppIDs"
  debug_session: ""

- truth: "aggregate_engagement only returns games where the searched tag appears in the game's top 20 tags"
  status: failed
  reason: "User reported: PUBG appears under Indie tag but Indie is not in PUBG's top 20 tags. Should only qualify games as befitting the searched tag if it's in the game's top 20 tag list"
  severity: major
  test: 9
  root_cause: "SteamSpy bulk tag endpoint (request=tag) returns ALL games with ANY association to the tag, regardless of weight. The bulk endpoint does NOT return per-game tag dicts — only the individual request=appdetails endpoint does. aggregate_engagement at steam_api.py:408-411 blindly includes every game without cross-validation. GameEngagementSummary schema has no tags field."
  artifacts:
    - path: "src/steam_api.py"
      issue: "aggregate_engagement lines 408-411 includes all games from bulk endpoint without tag cross-validation"
    - path: "src/schemas.py"
      issue: "GameEngagementSummary (lines 118-129) has no tags field for cross-validation"
  missing:
    - "Add per-game tag cross-validation: fetch appdetails for top N games, filter to those where searched tag appears in tags dict"
    - "Add tags field to GameEngagementSummary schema"
    - "Bound API calls — validate top N games (e.g., 100) not all 52k"
  debug_session: ""
