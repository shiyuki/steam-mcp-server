---
phase: 04-engagement-data
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/steam_api.py
  - tests/test_engagement.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All bulk results for a niche tag are cross-validated, not just top 100"
    - "TAG_CROSSVAL_THRESHOLD (5000) broad-tag bypass still works unchanged"
    - "All 138+ existing tests still pass"
  artifacts:
    - path: "src/steam_api.py"
      provides: "Uncapped tag cross-validation"
      contains: "candidates = bulk_games"
    - path: "tests/test_engagement.py"
      provides: "Test validates ALL bulk games, not capped at 100"
  key_links:
    - from: "src/steam_api.py"
      to: "bulk_games list"
      via: "candidates assignment at line 430"
      pattern: "candidates = bulk_games"
---

<objective>
Remove the TAG_VALIDATION_LIMIT=100 cap that prevents cross-validation of all bulk results.

Purpose: UAT test 8 found that aggregate_engagement only validates the top 100 games by owners, silently dropping all remaining games. For "Roguelike Deckbuilder" this means 364 of 464 bulk results are never checked. All bulk results should be cross-validated so every qualifying game is included.

Output: Updated steam_api.py and test_engagement.py -- uncapped validation for niche tags, broad-tag bypass preserved.
</objective>

<execution_context>
@C:\Users\shiyu\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiyu\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\shiyu\steam-mcp-server/.planning/STATE.md
@C:\Users\shiyu\steam-mcp-server/.planning/phases/04-engagement-data/04-UAT.md
@C:\Users\shiyu\steam-mcp-server/src/steam_api.py
@C:\Users\shiyu\steam-mcp-server/tests/test_engagement.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TAG_VALIDATION_LIMIT cap from steam_api.py</name>
  <files>src/steam_api.py</files>
  <action>
    1. Delete the TAG_VALIDATION_LIMIT constant at line 68 (`TAG_VALIDATION_LIMIT = 100`). Remove ONLY that line -- keep TAG_CROSSVAL_THRESHOLD = 5000 and the comment above it (line 70-71) intact.

    2. At line 430, change:
       `candidates = bulk_games[:TAG_VALIDATION_LIMIT]`
       to:
       `candidates = bulk_games`

       This removes the slice so ALL bulk games are cross-validated via appdetails, not just the top 100 by owners.

    3. Do NOT change anything related to TAG_CROSSVAL_THRESHOLD. The broad-tag bypass (>=5000 games skips validation) must remain as-is.

    4. Optionally update the comment on the line above (line 429) from "Validate top N candidates" to "Validate all candidates via appdetails (which includes per-game tags)" to reflect the new behavior.
  </action>
  <verify>
    Run: cd "C:/Users/shiyu/steam-mcp-server" && uv run python -c "from src.steam_api import TAG_CROSSVAL_THRESHOLD; print('TAG_CROSSVAL_THRESHOLD:', TAG_CROSSVAL_THRESHOLD)"
    Confirm TAG_CROSSVAL_THRESHOLD is 5000.
    Run: cd "C:/Users/shiyu/steam-mcp-server" && uv run python -c "from src.steam_api import TAG_VALIDATION_LIMIT" -- this should FAIL with ImportError, confirming the constant is removed.
    Grep src/steam_api.py for "TAG_VALIDATION_LIMIT" -- should return zero matches.
  </verify>
  <done>TAG_VALIDATION_LIMIT constant removed. Line 430 uses `candidates = bulk_games` (no slice). TAG_CROSSVAL_THRESHOLD unchanged at 5000.</done>
</task>

<task type="auto">
  <name>Task 2: Update test to validate all bulk games instead of enforcing cap</name>
  <files>tests/test_engagement.py</files>
  <action>
    1. In `test_aggregate_tag_validation_bounded` (starts around line 1506):
       - Remove the import: `from src.steam_api import TAG_VALIDATION_LIMIT`
       - Change the docstring from "Tag validation fetches at most TAG_VALIDATION_LIMIT appdetails" to "Tag validation fetches appdetails for all bulk results"
       - Change the assertion at line 1559 from:
         `assert appdetails_call_count <= TAG_VALIDATION_LIMIT`
         to:
         `assert appdetails_call_count == 200`
         (The bulk response generates 200 games, so ALL 200 should be validated.)
       - Keep the `assert isinstance(result, AggregateEngagementData)` assertion.

    2. Do NOT modify any other tests. All other test assertions should remain as-is.

    3. Run the FULL test suite to confirm all tests pass:
       cd "C:/Users/shiyu/steam-mcp-server" && uv run pytest tests/ -v

    All 138+ tests must pass. Zero failures.
  </action>
  <verify>
    Run: cd "C:/Users/shiyu/steam-mcp-server" && uv run pytest tests/ -v
    Confirm all tests pass with zero failures.
    Specifically confirm test_aggregate_tag_validation_bounded passes with the new assertion.
  </verify>
  <done>Test updated to assert all 200 bulk games are validated (not capped at 100). Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `TAG_VALIDATION_LIMIT` does not exist anywhere in src/steam_api.py
2. `TAG_CROSSVAL_THRESHOLD = 5000` is still present and unchanged
3. Line 430 area uses `candidates = bulk_games` (no slice)
4. test_aggregate_tag_validation_bounded asserts all bulk games validated
5. Full test suite passes: cd "C:/Users/shiyu/steam-mcp-server" && uv run pytest tests/ -v
</verification>

<success_criteria>
- TAG_VALIDATION_LIMIT removed entirely from source code
- All bulk results for niche tags are cross-validated (no cap)
- Broad-tag bypass (TAG_CROSSVAL_THRESHOLD >= 5000) unchanged
- All existing tests pass plus updated test confirms uncapped behavior
</success_criteria>

<output>
After completion, create `.planning/phases/04-engagement-data/04-05-SUMMARY.md`
</output>
