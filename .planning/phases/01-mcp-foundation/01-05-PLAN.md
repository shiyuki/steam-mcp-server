---
phase: 01-mcp-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - steam-mcp-server/README.md
autonomous: false

must_haves:
  truths:
    - "MCP server connects to Claude Desktop or Cursor"
    - "search_genre returns AppID list when asked for a genre"
    - "fetch_metadata returns game details when given an AppID"
    - "Invalid inputs return error messages, not crashes"
    - "Rate limiting prevents rapid-fire API abuse"
  artifacts:
    - path: "steam-mcp-server/README.md"
      provides: "Setup and usage instructions"
      contains: "claude_desktop_config.json"
  key_links:
    - from: "claude_desktop_config.json"
      to: "steam-mcp-server/src/server.py"
      via: "MCP server command"
      pattern: "uv.*run.*server"
---

<objective>
Create usage documentation and verify the MCP server works end-to-end with Claude/Cursor.

Purpose: Confirm the entire system works as specified - search by genre, fetch metadata, handle errors.
Output: Working MCP integration verified by human testing.
</objective>

<execution_context>
@C:\Users\shiyu\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\shiyu\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\shiyu\.planning\PROJECT.md
@C:\Users\shiyu\.planning\ROADMAP.md
@C:\Users\shiyu\.planning\phases\01-mcp-foundation\01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create README with setup instructions</name>
  <files>steam-mcp-server/README.md</files>
  <action>
Create steam-mcp-server/README.md with:

1. Project description (MCP server for Steam game research)
2. Prerequisites (Python 3.10+, uv, Steam API key)
3. Installation steps:
   - Clone/navigate to directory
   - Copy .env.example to .env
   - Add Steam API key
   - Run `uv sync`
4. Claude Desktop configuration:
   - Location: %APPDATA%\Claude\claude_desktop_config.json (Windows)
   - Config snippet for adding the MCP server
5. Cursor configuration (if different)
6. Usage examples:
   - "Search for roguelike games"
   - "Get metadata for AppID 646570"
7. Troubleshooting:
   - Check logs in stderr
   - Verify .env has valid API key
   - Restart Claude/Cursor after config change

README content:
```markdown
# Steam MCP Server

An MCP (Model Context Protocol) server that enables Claude and Cursor to search Steam games by genre and fetch game metadata.

## Prerequisites

- Python 3.10 or higher
- [uv](https://docs.astral.sh/uv/) package manager
- Steam Web API key ([get one here](https://steamcommunity.com/dev/apikey))

## Installation

1. Navigate to the project directory:
   ```bash
   cd steam-mcp-server
   ```

2. Copy the environment template and add your API key:
   ```bash
   cp .env.example .env
   # Edit .env and add your STEAM_API_KEY
   ```

3. Install dependencies:
   ```bash
   uv sync
   ```

## Claude Desktop Configuration

Add this server to your Claude Desktop config:

**Windows:** `%APPDATA%\Claude\claude_desktop_config.json`
**macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`

```json
{
  "mcpServers": {
    "steam": {
      "command": "uv",
      "args": ["--directory", "C:\\Users\\shiyu\\steam-mcp-server", "run", "python", "-m", "src.server"],
      "env": {
        "STEAM_API_KEY": "your_key_here"
      }
    }
  }
}
```

**Note:** Replace the path with your actual installation directory. On Windows, use double backslashes.

After editing, restart Claude Desktop completely.

## Cursor Configuration

Cursor uses the same MCP configuration format. Add to your Cursor settings or workspace `.cursor/mcp.json`.

## Available Tools

### search_genre

Search Steam games by genre/tag.

**Parameters:**
- `genre` (string, required): Steam tag to search (e.g., "Roguelike", "Action", "RPG")
- `limit` (integer, optional): Maximum results, 1-100, default 10

**Example prompt:** "Search for roguelike games on Steam"

### fetch_metadata

Get detailed metadata for a specific game.

**Parameters:**
- `appid` (integer, required): Steam AppID (e.g., 646570 for Slay the Spire)

**Example prompt:** "Get metadata for Steam AppID 646570"

## Troubleshooting

### Server not appearing in Claude

1. Check that the path in config is correct
2. Restart Claude Desktop completely (not just the window)
3. Check Claude's MCP logs for errors

### API errors

1. Verify your STEAM_API_KEY is valid
2. Check that .env file exists with the key
3. Look at server stderr output for detailed errors

### Rate limiting

The server enforces 1.5-second delays between API requests. If you see rate limit errors, wait a moment and retry.

## Development

Run the server directly for testing:
```bash
uv run python -m src.server
```

The server communicates via stdio (stdin/stdout for JSON-RPC, stderr for logs).
```

Adjust the Windows path in the config to match the actual installation location.
  </action>
  <verify>
    - README.md exists at steam-mcp-server/README.md
    - Contains Claude Desktop config example
    - Contains both tool descriptions
  </verify>
  <done>README.md created with setup instructions and Claude config</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify MCP server works with Claude/Cursor</name>
  <what-built>
Complete MCP server with:
- search_genre tool (searches SteamSpy by tag)
- fetch_metadata tool (fetches Steam Store details)
- Rate limiting (1.5s between requests)
- Error handling with structured responses
  </what-built>
  <how-to-verify>
1. **Setup:**
   - Ensure .env file exists in steam-mcp-server/ with valid STEAM_API_KEY
   - Add MCP server to Claude Desktop config (see README.md)
   - Restart Claude Desktop completely

2. **Test search_genre:**
   - Ask Claude: "Search for roguelike games on Steam"
   - Expected: List of AppIDs (like 646570, 247080, etc.)

3. **Test fetch_metadata:**
   - Ask Claude: "Get metadata for Steam AppID 646570"
   - Expected: Game details (Slay the Spire, developer Mega Crit, etc.)

4. **Test error handling:**
   - Ask Claude: "Get metadata for Steam AppID -1"
   - Expected: Error message about invalid AppID, not a crash

5. **Verify logging:**
   - Check Claude's MCP server logs (if available) or server stderr
   - Should see INFO logs about requests, no stdout pollution
  </how-to-verify>
  <resume-signal>Type "verified" if all tests pass, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
Human verification required - this plan completes with a checkpoint.
</verification>

<success_criteria>
1. README.md provides clear setup instructions
2. Claude can successfully call search_genre and get results
3. Claude can successfully call fetch_metadata and get game details
4. Invalid inputs return friendly error messages
5. Server logs appear in stderr only
</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-foundation/01-05-SUMMARY.md`
</output>
